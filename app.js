const carNamesArray = ["Porsche", "Honda", "Camaro", "Fruit"];

module.exports = {
    carNames: carNamesArray,
    func: function (one, two) {
        const third = one + two
        return third;

    }
};
